'use strict';
//ternary es una abreviación del if else
//true ? false: 

let num = 11;
let response = (num % 2 === 0) ? "es par" : "es impar";//se pueden colocar condiciones dobles
console.log(response);